---
phase: 02-database-and-affiliate-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Breadcrumbs.tsx
  - src/components/ui/FtcDisclosure.tsx
  - src/components/ui/BackToMap.tsx
  - src/components/detail/MiniMap.tsx
  - tests/breadcrumbs.test.ts
autonomous: true
requirements: [PAGE-05]

must_haves:
  truths:
    - "Breadcrumb navigation renders Home > Region > Name on detail pages"
    - "Breadcrumb component emits BreadcrumbList JSON-LD structured data"
    - "Breadcrumb region links point to future SEO listing URLs (e.g. /festivals/southeast-asia)"
    - "FTC disclosure appears near affiliate CTAs as small inline text"
    - "Back to Map button floats on detail pages and preserves last viewport"
  artifacts:
    - path: "src/components/ui/Breadcrumbs.tsx"
      provides: "Visual breadcrumb nav + BreadcrumbList JSON-LD"
      contains: "BreadcrumbList"
    - path: "src/components/ui/FtcDisclosure.tsx"
      provides: "Inline affiliate disclosure badge"
      contains: "Affiliate"
    - path: "src/components/ui/BackToMap.tsx"
      provides: "Floating back-to-map navigation button"
      contains: "Back to Map"
    - path: "src/components/detail/MiniMap.tsx"
      provides: "Small embedded MapLibre map for detail pages"
      contains: "maplibregl"
    - path: "tests/breadcrumbs.test.ts"
      provides: "Unit tests for breadcrumb rendering and JSON-LD output"
  key_links:
    - from: "src/components/ui/Breadcrumbs.tsx"
      to: "JSON-LD script tag"
      via: "dangerouslySetInnerHTML with BreadcrumbList schema"
      pattern: "BreadcrumbList"
---

<objective>
Create the shared UI components needed by both event and wildlife detail pages: Breadcrumbs (visual + JSON-LD structured data), FTC affiliate disclosure, Back to Map floating button, and a reusable MiniMap component.

Purpose: These components are shared across /event/[slug] and /wildlife/[slug] pages. Building them first as a Wave 1 plan allows both detail page plans (02-03, 02-04) to run in parallel in Wave 2 without duplicating component work.

Output: Four reusable components ready for import by SSG detail pages.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-and-affiliate-infrastructure/02-RESEARCH.md
@.planning/phases/02-database-and-affiliate-infrastructure/02-CONTEXT.md
@src/components/ui/CrowdBadge.tsx
@src/components/panel/AffiliateLinks.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Breadcrumbs component with BreadcrumbList JSON-LD and tests</name>
  <files>
    src/components/ui/Breadcrumbs.tsx
    tests/breadcrumbs.test.ts
  </files>
  <action>
1. Create `src/components/ui/Breadcrumbs.tsx`:
   - Props: `items: Array<{ name: string; href?: string }>` — last item has no href (current page)
   - Render a `<nav aria-label="Breadcrumb">` with an `<ol>` of `<li>` items
   - Each item except the last is an `<a>` (use Next.js `Link`), last item is a `<span aria-current="page">`
   - Separator: `>` or `/` between items, styled with Tailwind (text-sm text-gray-500)
   - Breadcrumb pattern per user decision: Home > Region > Event/Species Name
   - Region links point to future SEO listing URLs (e.g., `/festivals/southeast-asia`) — these won't resolve yet (Phase 4), but the URLs are reserved now per user decision
   - Render BreadcrumbList JSON-LD as a `<script type="application/ld+json">` tag below the nav
   - JSON-LD follows Google's BreadcrumbList spec from RESEARCH.md Pattern 5
   - Apply XSS prevention: `.replace(/</g, '\\u003c')` on JSON.stringify output
   - Use site URL `https://happeningnow.travel` as base for absolute URLs in JSON-LD

2. Create `tests/breadcrumbs.test.ts`:
   - Test: renders correct number of breadcrumb items
   - Test: last item is not a link (span with aria-current)
   - Test: JSON-LD script tag contains BreadcrumbList type
   - Test: JSON-LD contains correct number of ListItem entries
   - Test: handles single-item breadcrumb (just Home)
  </action>
  <verify>
    <automated>npx vitest run tests/breadcrumbs.test.ts</automated>
    <manual>None needed</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Breadcrumbs component renders visual nav + BreadcrumbList JSON-LD, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create FtcDisclosure, BackToMap, and MiniMap components</name>
  <files>
    src/components/ui/FtcDisclosure.tsx
    src/components/ui/BackToMap.tsx
    src/components/detail/MiniMap.tsx
  </files>
  <action>
1. Create `src/components/ui/FtcDisclosure.tsx`:
   - Small inline component: renders "Affiliate link" as a subtle badge or small text
   - Tailwind styling: text-xs text-gray-400, possibly with a small info icon
   - Per user decision: "Small inline FTC disclosure note near each affiliate CTA"
   - Used next to or below affiliate link buttons

2. Create `src/components/ui/BackToMap.tsx`:
   - 'use client' component (needs router)
   - Floating button: fixed or sticky position at bottom-right (or bottom-center)
   - Text: "Back to Map" with a map icon
   - On click: navigate to `/` preserving last viewport/filters via URL search params
   - Store last viewport in localStorage or URL params when navigating away from map
   - Per user decision: "Floating 'Back to Map' button on all detail pages, preserving last viewport/filters"
   - Tailwind: rounded-full, shadow-lg, bg-white, hover:bg-gray-50, z-50

3. Create `src/components/detail/MiniMap.tsx`:
   - 'use client' component loaded via `next/dynamic` with `ssr: false` in consuming pages
   - Props: `coordinates: [lng: number, lat: number]`, optional `zoom?: number` (default 10)
   - Renders a small MapLibre instance (aspect-ratio: 16/9, max-height ~200px)
   - Shows a single marker at the given coordinates
   - Non-interactive: `interactive: false` or minimal controls (no zoom/pan)
   - Uses same OpenFreeMap tile URL from `src/lib/constants.ts`
   - Per user decision for event pages: "mini embedded MapLibre map pinpointing the event"
   - Per user decision for wildlife pages: "Mini animated map showing the migration route with marked viewing locations"
   - For wildlife, accept optional `routeCoordinates: number[][]` prop to render a LineString overlay
  </action>
  <verify>
    <automated>npx vitest run</automated>
    <manual>None needed - these are simple presentational components</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>FtcDisclosure renders inline affiliate badge, BackToMap provides floating navigation, MiniMap renders small MapLibre instance with marker (and optional route line)</done>
</task>

</tasks>

<verification>
- `src/components/ui/Breadcrumbs.tsx` renders BreadcrumbList JSON-LD in a script tag
- `src/components/ui/FtcDisclosure.tsx` renders small "Affiliate link" disclosure text
- `src/components/ui/BackToMap.tsx` navigates to `/` with viewport preservation
- `src/components/detail/MiniMap.tsx` renders a small MapLibre map with marker
- `npx vitest run` passes all tests including breadcrumbs tests
</verification>

<success_criteria>
- Breadcrumbs component renders Home > Region > Name pattern with JSON-LD structured data
- FTC disclosure is a small, non-intrusive inline element
- Back to Map floats on detail pages and preserves map state
- MiniMap shows a focused map with a point marker and optional route line
- All breadcrumb tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-and-affiliate-infrastructure/02-02-SUMMARY.md`
</output>
