---
phase: 02-database-and-affiliate-infrastructure
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/get_event_with_coords.sql
  - src/lib/supabase/queries.ts
  - src/lib/supabase/types.ts
  - src/app/event/[slug]/page.tsx
  - src/components/detail/EventContent.tsx
autonomous: true
requirements: [PAGE-03, PAGE-04, PAGE-05, PERF-02, PERF-03]
gap_closure: true

must_haves:
  truths:
    - "Visiting /event/[slug] renders a MiniMap pinpointing the event location"
    - "Event coordinates are extracted from PostGIS geometry via ST_X/ST_Y, not hardcoded or approximated"
  artifacts:
    - path: "supabase/functions/get_event_with_coords.sql"
      provides: "SQL RPC returning event row plus lng/lat columns"
      contains: "ST_X"
    - path: "src/lib/supabase/queries.ts"
      provides: "getEventBySlug calls RPC and returns EventWithCoords"
      contains: "get_event_with_coords"
    - path: "src/app/event/[slug]/page.tsx"
      provides: "Passes coordinates prop to EventContent"
      contains: "coordinates"
  key_links:
    - from: "src/app/event/[slug]/page.tsx"
      to: "src/components/detail/EventContent.tsx"
      via: "coordinates prop"
      pattern: "coordinates=.*\\[.*lng.*lat"
    - from: "src/lib/supabase/queries.ts"
      to: "supabase.rpc('get_event_with_coords')"
      via: "Supabase RPC call"
      pattern: "rpc\\('get_event_with_coords'"
---

<objective>
Close the event MiniMap gap: create a PostGIS RPC that extracts event point coordinates (ST_X/ST_Y) and wire them through to the MiniMap component on event detail pages.

Purpose: The event detail page was built in 02-03 but the MiniMap section is silently skipped because PostGIS geometry (WKB format) cannot be extracted directly via supabase-js. This gap closure adds the missing SQL RPC and data plumbing.

Output: MiniMap renders on all /event/[slug] pages showing the event's location.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-and-affiliate-infrastructure/02-03-SUMMARY.md
@supabase/functions/get_wildlife_with_route.sql
@src/lib/supabase/queries.ts
@src/lib/supabase/types.ts
@src/app/event/[slug]/page.tsx
@src/components/detail/EventContent.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create get_event_with_coords SQL RPC and update TypeScript types</name>
  <files>
    supabase/functions/get_event_with_coords.sql
    src/lib/supabase/types.ts
  </files>
  <action>
Create a new SQL RPC `get_event_with_coords` following the same pattern as `get_wildlife_with_route.sql`.

The function accepts `event_slug TEXT` and returns a single row with all events table columns plus two additional columns: `lng DOUBLE PRECISION` (from `ST_X(location)`) and `lat DOUBLE PRECISION` (from `ST_Y(location)`). Use `LANGUAGE SQL STABLE`. Use bare `ST_X`/`ST_Y` (no schema prefix) — matching the existing convention where `ST_AsGeoJSON` is used without prefix in `get_wildlife_with_route.sql`. Include `LIMIT 1` since slugs are unique.

In `src/lib/supabase/types.ts`:
- Add an `EventWithCoords` interface that extends `Event` with `lng: number` and `lat: number` fields (omitting the `location` field since it's replaced by the extracted coordinates).
- Add `get_event_with_coords` to the `Database.public.Functions` record with `Args: { event_slug: string }` and `Returns: EventWithCoords[]`.
  </action>
  <verify>
    <automated>cd /Users/oliverhovey/Documents/vibes/HappeningNow && cat supabase/functions/get_event_with_coords.sql | grep -q "ST_X" && cat src/lib/supabase/types.ts | grep -q "EventWithCoords" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SQL RPC file exists with ST_X/ST_Y extraction; EventWithCoords type defined in types.ts; Database Functions record includes get_event_with_coords</done>
</task>

<task type="auto">
  <name>Task 2: Wire coordinates through getEventBySlug -> EventPage -> EventContent -> MiniMap</name>
  <files>
    src/lib/supabase/queries.ts
    src/app/event/[slug]/page.tsx
  </files>
  <action>
In `src/lib/supabase/queries.ts`:
- Update `getEventBySlug` to call `supabase.rpc('get_event_with_coords', { event_slug: slug })` instead of `supabase.from('events').select('*').eq('slug', slug).single()`. The RPC returns an array; take `data[0]`. Return type changes to `EventWithCoords | null`. Import `EventWithCoords` from types.
- Keep `getAllEventSlugs` unchanged (it only needs slugs, no coordinates).
- Keep `getNearbyEvents` unchanged (it does not need coordinates).

In `src/app/event/[slug]/page.tsx`:
- Remove the TODO comment block about coordinates (lines 83-87).
- After calling `getEventBySlug(slug)`, extract coordinates: `const coordinates: [number, number] | undefined = event.lng != null && event.lat != null ? [event.lng, event.lat] : undefined;`
- Pass `coordinates={coordinates}` to the `<EventContent>` component.
- No changes needed to `EventContent.tsx` — it already accepts and renders coordinates when truthy.

Run `npx vitest run` to verify all 90 existing tests still pass (the test suite does not call the actual RPC, so existing tests should be unaffected).
  </action>
  <verify>
    <automated>cd /Users/oliverhovey/Documents/vibes/HappeningNow && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>getEventBySlug uses the new RPC; EventPage computes and passes coordinates to EventContent; MiniMap will render on event detail pages when the RPC is deployed to Supabase; all existing tests pass</done>
</task>

</tasks>

<verification>
- `supabase/functions/get_event_with_coords.sql` exists and contains ST_X/ST_Y
- `src/lib/supabase/types.ts` exports `EventWithCoords` with `lng` and `lat`
- `src/lib/supabase/queries.ts` calls `rpc('get_event_with_coords')`
- `src/app/event/[slug]/page.tsx` passes `coordinates` prop to `EventContent`
- No TODO comment about coordinates remains in page.tsx
- All 90 existing tests pass
</verification>

<success_criteria>
The event detail page data pipeline now extracts coordinates from PostGIS geometry via an SQL RPC. When `get_event_with_coords` is deployed to Supabase, the MiniMap section will render on all /event/[slug] pages showing the event's location marker.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-and-affiliate-infrastructure/02-05-SUMMARY.md`
</output>
