---
phase: 02-database-and-affiliate-infrastructure
plan: 04
type: execute
wave: 2
depends_on: [02-02, 02-03]
files_modified:
  - src/app/wildlife/[slug]/page.tsx
  - src/components/detail/WildlifeHero.tsx
  - src/components/detail/WildlifeContent.tsx
  - src/lib/structured-data.ts
autonomous: true
requirements: [PAGE-03, PAGE-04]

must_haves:
  truths:
    - "Visiting /wildlife/[slug] renders a full wildlife detail page with hero image, description, viewing info, migration map, and affiliate CTAs"
    - "Wildlife pages have JSON-LD Event structured data (using the Event schema for wildlife spectacles)"
    - "Wildlife pages have Open Graph meta tags with wildlife photo as OG image"
    - "Wildlife pages have breadcrumb navigation (Home > Region > Species Name)"
    - "Mini map on wildlife page shows migration route LineString with viewing location markers"
  artifacts:
    - path: "src/app/wildlife/[slug]/page.tsx"
      provides: "SSG wildlife detail page with generateStaticParams and generateMetadata"
      contains: "generateStaticParams"
    - path: "src/components/detail/WildlifeHero.tsx"
      provides: "Full-width hero image with species name and peak months"
    - path: "src/components/detail/WildlifeContent.tsx"
      provides: "Wildlife description, peak months, viewing info, migration map, affiliate links"
    - path: "src/lib/structured-data.ts"
      provides: "buildWildlifeJsonLd helper"
      contains: "buildWildlifeJsonLd"
  key_links:
    - from: "src/app/wildlife/[slug]/page.tsx"
      to: "src/lib/supabase/queries.ts"
      via: "getWildlifeBySlug for page data, getAllWildlifeSlugs for generateStaticParams"
      pattern: "getWildlifeBySlug|getAllWildlifeSlugs"
    - from: "src/app/wildlife/[slug]/page.tsx"
      to: "JSON-LD script tag"
      via: "Event schema adapted for wildlife spectacles"
      pattern: "application/ld\\+json"
---

<objective>
Create the `/wildlife/[slug]` SSG detail pages with wildlife viewing information, migration route map, JSON-LD structured data, Open Graph meta tags, and breadcrumb navigation.

Purpose: Wildlife detail pages parallel the event pages but include migration route visualization. They use the same shared components (Breadcrumbs, FTC disclosure, BackToMap, MiniMap) and server query helpers created in plans 02-02 and 02-03.

Output: Working `/wildlife/[slug]` pages statically generated for all migration routes in the database.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-and-affiliate-infrastructure/02-RESEARCH.md
@.planning/phases/02-database-and-affiliate-infrastructure/02-CONTEXT.md
@.planning/phases/02-database-and-affiliate-infrastructure/02-02-SUMMARY.md
@.planning/phases/02-database-and-affiliate-infrastructure/02-03-SUMMARY.md
@src/lib/supabase/types.ts
@src/lib/supabase/queries.ts
@src/lib/structured-data.ts
@src/lib/affiliates.ts
@src/components/ui/Breadcrumbs.tsx
@src/components/ui/FtcDisclosure.tsx
@src/components/ui/BackToMap.tsx
@src/components/detail/MiniMap.tsx
@src/components/detail/EventHero.tsx
@src/components/detail/EventContent.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wildlife hero, content components, and JSON-LD helper</name>
  <files>
    src/components/detail/WildlifeHero.tsx
    src/components/detail/WildlifeContent.tsx
    src/lib/structured-data.ts
  </files>
  <action>
1. Create `src/components/detail/WildlifeHero.tsx`:
   - Same structure as EventHero but adapted for wildlife/migration routes
   - Full-width hero image from migration_route.image_url
   - If no image: green gradient background (wildlife category color)
   - Overlay: species name + route name, peak months formatted
   - Per user decision: "Same content structure as event pages where applicable"

2. Create `src/components/detail/WildlifeContent.tsx`:
   - Description text from migration_route.description
   - Peak viewing months section with month names
   - MiniMap with route coordinates: pass `routeCoordinates` prop to MiniMap to render the LineString overlay showing the migration path
   - Per user decision: "Mini animated map showing the migration route with marked viewing locations, month-aware with current position highlighted"
   - Compute the month-aware active position along the route:
     * Determine the current month (or accept a `selectedMonth` prop for future interactivity)
     * Map the current month to a position along the routeCoordinates array. Simple approach: if the route has N waypoints and the migration spans M months (from peak_months), interpolate linearly — month index within the migration window maps to a proportional index along the route coordinates. If the current month falls outside the migration window, highlight the nearest endpoint.
     * Pass the computed `[lng, lat]` as the `activePosition` prop to MiniMap so the highlighted dot appears at the month-appropriate waypoint
   - Pass viewing location waypoints (if available from route data) as `waypointLabels` to MiniMap
   - Affiliate CTAs (GetYourGuide for wildlife tours in the region) with FTC disclosure
   - Note: Route coordinates need to be extracted from PostGIS. Update `getWildlifeBySlug` in queries.ts to also return route geometry as GeoJSON (use `ST_AsGeoJSON(route)::json as route_geojson` or create an RPC).

3. Update `src/lib/structured-data.ts`:
   - Add `buildWildlifeJsonLd(route: MigrationRoute)` function
   - Use Event schema type (wildlife spectacles are events in schema.org terms)
   - name: route.name, description: route.description
   - startDate/endDate derived from peak_months array (first and last month)
   - eventAttendanceMode: OfflineEventAttendanceMode
   - location: general region description (species name + "migration")
   - image: route.image_url
  </action>
  <verify>
    <automated>npx vitest run tests/structured-data.test.ts</automated>
    <manual>None needed</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Wildlife hero and content components created, buildWildlifeJsonLd helper added to structured-data.ts</done>
</task>

<task type="auto">
  <name>Task 2: Create /wildlife/[slug] SSG page with generateMetadata and JSON-LD</name>
  <files>
    src/app/wildlife/[slug]/page.tsx
    src/lib/supabase/queries.ts
  </files>
  <action>
1. Update `src/lib/supabase/queries.ts` if needed:
   - Ensure `getWildlifeBySlug` returns route geometry as GeoJSON coordinates (for MiniMap)
   - May need to create a small RPC or use a select that includes `ST_AsGeoJSON(route)::json` — if supabase-js can't do this directly, create `supabase/functions/get_wildlife_with_route.sql` RPC that returns the migration_route row with route as GeoJSON

2. Create `src/app/wildlife/[slug]/page.tsx`:
   - `generateStaticParams()`: calls `getAllWildlifeSlugs()`, returns `{ slug }[]`
   - `generateMetadata({ params })`: await params (Next.js 15+), calls `getWildlifeBySlug`, returns Metadata with title (species + route name), description, openGraph with wildlife photo as OG image (per user decision: "OG images use the wildlife photo directly")
   - Page component (async Server Component):
     * Await params, fetch wildlife route via `getWildlifeBySlug(slug)`
     * If not found, call `notFound()`
     * Render: Breadcrumbs (Home > Region/Species > Route Name), WildlifeHero, WildlifeContent (with route data + GeoJSON coordinates), BackToMap
     * Render JSON-LD Event script tag via buildWildlifeJsonLd with XSS prevention
     * Render BreadcrumbList JSON-LD via Breadcrumbs component

3. Follow the same page structure pattern as `/event/[slug]/page.tsx` for consistency.
  </action>
  <verify>
    <automated>npx vitest run</automated>
    <manual>Run `npm run dev`, visit `/wildlife/{any-slug}` — verify hero image, breadcrumbs, JSON-LD in page source, OG tags, migration route on mini map, affiliate links with FTC disclosure</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>Visiting /wildlife/[slug] shows complete wildlife detail page with hero, breadcrumbs, JSON-LD, OG meta tags, migration route mini map, and affiliate CTAs with FTC disclosure</done>
</task>

</tasks>

<verification>
- `src/app/wildlife/[slug]/page.tsx` exports generateStaticParams and generateMetadata
- Page source contains `<script type="application/ld+json">` with Event schema for wildlife
- Page `<head>` contains og:title, og:description, og:image meta tags
- Breadcrumbs render Home > Species/Region > Route Name
- MiniMap shows migration route LineString
- Affiliate links have FTC disclosure nearby
- `npx vitest run` passes all tests
</verification>

<success_criteria>
- Wildlife detail pages render with hero, description, peak months, migration map, and affiliate CTAs
- JSON-LD uses Event schema adapted for wildlife spectacles
- Open Graph meta tags use wildlife photo directly (per user decision)
- Breadcrumbs follow Home > Region > Species Name pattern
- generateStaticParams generates pages for all migration routes
- MiniMap shows the migration route as a LineString overlay
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-and-affiliate-infrastructure/02-04-SUMMARY.md`
</output>
