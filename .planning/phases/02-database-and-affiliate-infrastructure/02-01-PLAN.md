---
phase: 02-database-and-affiliate-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/get_events_bbox.sql
  - src/app/api/events/route.ts
  - src/components/map/MapView.tsx
  - src/lib/supabase/types.ts
  - tests/api/events-bbox.test.ts
autonomous: true
requirements: [PERF-02, PERF-03]

must_haves:
  truths:
    - "Map loads events from bbox API instead of full-table RPC"
    - "Panning the map re-fetches only viewport events via debounced moveend"
    - "PostGIS bounding-box query uses GiST index and returns in under 200ms"
    - "API validates query parameters and returns structured errors for invalid input"
  artifacts:
    - path: "supabase/functions/get_events_bbox.sql"
      provides: "PostGIS bbox RPC function with month and category filters"
      contains: "get_events_bbox"
    - path: "src/app/api/events/route.ts"
      provides: "Route Handler with Zod validation"
      exports: ["GET"]
    - path: "src/components/map/MapView.tsx"
      provides: "Bbox-aware map data fetching with debounced moveend"
      contains: "moveend"
    - path: "tests/api/events-bbox.test.ts"
      provides: "Unit tests for Route Handler validation"
  key_links:
    - from: "src/components/map/MapView.tsx"
      to: "/api/events"
      via: "fetch on debounced moveend with AbortController"
      pattern: "fetch.*api/events"
    - from: "src/app/api/events/route.ts"
      to: "supabase.rpc('get_events_bbox')"
      via: "Supabase RPC call"
      pattern: "rpc.*get_events_bbox"
---

<objective>
Create the PostGIS bounding-box RPC function, expose it via a Next.js Route Handler with Zod validation, and switch MapView from the Phase 1 full-table `get_events_geojson` RPC to live bbox API calls on debounced moveend.

Purpose: This is the clean break from Phase 1 static data loading to production spatial queries. The bbox API ensures only viewport events are fetched, meeting the PERF-02 (<2s map load) and PERF-03 (<200ms API response) requirements.

Output: Working bbox pipeline from map moveend -> Route Handler -> PostGIS RPC -> GeoJSON response -> map source update.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-and-affiliate-infrastructure/02-RESEARCH.md
@.planning/phases/01-map-foundation/01-02-SUMMARY.md
@.planning/phases/01-map-foundation/01-04-SUMMARY.md
@supabase/schema.sql
@supabase/functions/get_events_geojson.sql
@src/components/map/MapView.tsx
@src/lib/supabase/types.ts
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bbox RPC function and Route Handler with tests</name>
  <files>
    supabase/functions/get_events_bbox.sql
    src/app/api/events/route.ts
    src/lib/supabase/types.ts
    tests/api/events-bbox.test.ts
  </files>
  <action>
1. Create `supabase/functions/get_events_bbox.sql` with the RPC function from RESEARCH.md Pattern 1:
   - Parameters: min_lng, min_lat, max_lng, max_lat (float), filter_month (int, optional), filter_category (text, optional)
   - Uses `&& ST_MakeEnvelope(min_lng, min_lat, max_lng, max_lat, 4326)` for GiST index scan
   - Month filter handles year-boundary wrapping (start_month > end_month)
   - Returns json_build_object with FeatureCollection wrapping json_agg of Features
   - COALESCE to empty array to prevent null (established Phase 1 pattern)
   - Language: sql, volatility: STABLE

2. Create `src/app/api/events/route.ts` Route Handler:
   - Install zod if not already a direct dependency: `npm install zod`
   - Zod schema validates: bbox (string of 4 comma-separated numbers), month (1-12 int, optional), category (enum 'festival'|'wildlife', optional)
   - GET handler: parse searchParams with safeParse, return 400 on validation failure
   - Call createServerClient().rpc('get_events_bbox', { ...params })
   - Return GeoJSON with Cache-Control: `public, s-maxage=60, stale-while-revalidate=300`
   - Return 500 with error message on Supabase errors

3. Update `src/lib/supabase/types.ts`:
   - Add `get_events_bbox` to Database.public.Functions with Args type matching the 6 parameters and Returns: EventGeoJSON

4. Create `tests/api/events-bbox.test.ts`:
   - Test Zod schema validation: valid bbox parses correctly, missing bbox returns error, non-numeric bbox values rejected, month out of range rejected, invalid category rejected
   - Extract the Zod schema to a named export from route.ts for testability, or test via direct import
   - Do NOT test actual Supabase calls (unit tests only)
  </action>
  <verify>
    <automated>npx vitest run tests/api/events-bbox.test.ts</automated>
    <manual>Review get_events_bbox.sql for correct ST_MakeEnvelope parameter order (xmin=min_lng, ymin=min_lat, xmax=max_lng, ymax=max_lat)</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Bbox RPC SQL file exists, Route Handler validates params and calls RPC, type definitions updated, validation tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Switch MapView from full-table RPC to bbox API with debounced moveend</name>
  <files>
    src/components/map/MapView.tsx
  </files>
  <action>
1. Replace the existing Supabase RPC fetch (`get_events_geojson`) in MapView.tsx with a bbox API fetch:
   - Remove direct Supabase client import and the `get_events_geojson` RPC call
   - Add a `fetchBboxEvents` function (per RESEARCH.md code example):
     * Gets viewport bounds from `map.getBounds()` -> `getWest(), getSouth(), getEast(), getNorth()`
     * Builds URLSearchParams with bbox, optional month, optional category
     * Fetches `/api/events?${params}` with AbortController signal
     * Returns parsed JSON

2. Add debounced moveend handler:
   - Use useEffect with map ref dependency
   - On `moveend`: clear previous timer, abort previous request, start new 350ms timer
   - After debounce: fetch bbox events, update GeoJSON source via `source.setData(data)`
   - On cleanup: remove listener, clear timer, abort controller
   - Handle AbortError silently (expected during rapid panning)

3. Initial load:
   - Fire an initial bbox fetch after map `load` event (or reuse moveend logic)
   - Remove any existing `useEffect` that calls `get_events_geojson` on mount

4. Keep the existing source-level filtering pipeline (`filterGeoJSON` + `setData`) for client-side month/category refinement between API calls. The API provides the viewport data; client filters refine it without new fetches when only filter controls change.

5. Ensure selectedMonth and activeCategories changes also trigger a new bbox fetch (not just moveend), since the API supports server-side filtering.
  </action>
  <verify>
    <automated>npx vitest run</automated>
    <manual>Run `npm run dev`, pan the map, verify Network tab shows `/api/events?bbox=...` requests on moveend (debounced), verify markers update correctly</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>MapView fetches events from /api/events bbox endpoint on debounced moveend, initial load uses bbox, no more get_events_geojson RPC calls, filter changes trigger re-fetch</done>
</task>

</tasks>

<verification>
- `supabase/functions/get_events_bbox.sql` exists and contains valid SQL with ST_MakeEnvelope, COALESCE, and STABLE volatility
- `src/app/api/events/route.ts` exports GET handler with Zod validation
- `src/components/map/MapView.tsx` no longer imports or calls `get_events_geojson`
- `src/components/map/MapView.tsx` contains `moveend` handler fetching from `/api/events`
- `npx vitest run` passes all tests including new bbox validation tests
- User SQL action: Run `get_events_bbox.sql` in Supabase SQL Editor, then run `EXPLAIN ANALYZE SELECT * FROM get_events_bbox(-180, -90, 180, 90, NULL, NULL);` to verify <200ms and GiST index usage
</verification>

<success_criteria>
- PostGIS bbox RPC function returns GeoJSON FeatureCollection for any viewport
- Route Handler validates bbox, month, category params with Zod and returns proper errors
- MapView loads events via /api/events on initial render and on every debounced moveend
- No references to get_events_geojson remain in MapView
- All existing tests plus new validation tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-and-affiliate-infrastructure/02-01-SUMMARY.md`
</output>
