---
phase: 03-visual-differentiators
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/map/MapView.tsx
  - src/components/map/MapShell.tsx
  - src/app/destination/[slug]/page.tsx
autonomous: true
gap_closure: true
requirements:
  - DEST-01
  - CROWD-01
  - CROWD-02
  - CROWD-03
  - CROWD-04
  - CROWD-05
  - WILD-01
  - WILD-02
  - WILD-03
  - WILD-04
  - WILD-05
  - DEST-02
  - DEST-03
  - DEST-04
  - DEST-05
  - DEST-06
  - DEST-07
  - PERF-01

must_haves:
  truths:
    - "Navigating to /destination/[slug] from the heatmap popup or any destination link triggers map.flyTo() to the destination coordinates"
    - "Destination links include lat, lng, and zoom as URL search params"
    - "MapView reads ?lat=&lng=&zoom= from URL on load and calls map.flyTo() with those coordinates"
  artifacts:
    - path: "src/components/map/MapView.tsx"
      provides: "URL search param reading and flyTo on map load"
      contains: "flyTo.*lat.*lng"
    - path: "src/app/destination/[slug]/page.tsx"
      provides: "Destination page with flyTo coordinates passed via BackToMap or navigation"
  key_links:
    - from: "heatmap popup destination link"
      to: "/?lat=X&lng=Y&zoom=Z"
      via: "URL search params"
      pattern: "lat=.*lng=.*zoom="
    - from: "MapView.tsx"
      to: "map.flyTo"
      via: "useSearchParams reading lat/lng/zoom"
      pattern: "searchParams.*flyTo"
---

<objective>
Close DEST-01 gap: add map flyTo animation when navigating to/from destination pages, and investigate+fix the heatmap not displaying visually.

Purpose: DEST-01 requires "map flyTo animation on navigation" but the current implementation has no flyTo trigger. The heatmap popup also has no link to the destination page. Additionally, the user reports the heatmap is not displaying — this needs investigation.

Output: MapView reads URL search params for destination flyTo, heatmap popup links to destination pages with coordinates, and heatmap rendering issues are diagnosed and fixed.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-differentiators/03-01-SUMMARY.md
@.planning/phases/03-visual-differentiators/03-03-SUMMARY.md
@.planning/phases/03-visual-differentiators/03-VERIFICATION.md
@src/components/map/MapView.tsx
@src/components/map/MapShell.tsx
@src/app/destination/[slug]/page.tsx
@src/lib/map/heatmap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flyTo from URL search params and destination links with coordinates</name>
  <files>
    src/components/map/MapView.tsx
    src/components/map/MapShell.tsx
  </files>
  <action>
    **Part A: MapView reads URL search params for flyTo on load**

    1. In `MapView.tsx`, add a prop `flyToTarget?: { lat: number; lng: number; zoom: number }` to the MapView component props.

    2. In the `map.on('load', ...)` handler (around line 253), after map setup, check if `flyToTarget` is provided. If so, call `map.flyTo({ center: [flyToTarget.lng, flyToTarget.lat], zoom: flyToTarget.zoom, duration: 2000 })`.

    3. In `MapShell.tsx` (the client wrapper that uses `next/dynamic` to load MapView), use `useSearchParams()` from `next/navigation` to read `lat`, `lng`, and `zoom` query params. Parse them as floats. If all three are present and valid numbers, pass them as the `flyToTarget` prop to MapView. After reading and passing the params, clear the URL search params using `router.replace(pathname)` via `useRouter` and `usePathname` so the flyTo only triggers once.

    **Part B: Heatmap popup links to destination pages with coordinates**

    4. In `MapView.tsx`, in the heatmap click handler (around line 320-379), add a link to the destination page BELOW the "Find quieter alternatives" button. The link should be:
    ```html
    <a href="/destination/${nearest.slug}" style="display:block; text-align:center; margin-top:6px; font-size:12px; color:#3b82f6; text-decoration:underline;">
      View ${nearest.name} details
    </a>
    ```
    This is a simple page link (no search params needed here since the destination page itself is the target, not a return-to-map flyTo).

    **Part C: BackToMap links back to map with destination flyTo coordinates**

    5. In the destination page (`src/app/destination/[slug]/page.tsx`), the `BackToMap` component should include the destination's coordinates in its link back to the map. Check how `BackToMap` works — if it uses `saveViewport`/`getSavedViewport` from localStorage, ensure that when a user arrives at a destination page, the destination's lat/lng are saved so that going back to the map shows that destination area. If BackToMap uses a simple `href="/"`, update it to link to `/?lat=${destination.lat}&lng=${destination.lng}&zoom=8` so the map flyTo triggers on return.

    However, the destination object from Supabase may not have lat/lng directly available (it has PostGIS geometry). Check `getDestinationBySlug` return type — if it returns `DestinationWithCoords` (which has `lat`, `lng`), use those. If it returns the base `Destination` type (no coords), add a new server-side query or use the `get_destinations_with_coords` RPC to get coords for the specific destination, OR add a separate lightweight RPC/query that returns just the coords for one slug.

    **Important:** Do NOT break any existing map functionality. The flyTo should only trigger when URL params are present. The heatmap popup link is additive.
  </action>
  <verify>
    <automated>cd /Users/oliverhovey/Documents/vibes/HappeningNow && npx vitest run 2>&1 | tail -5</automated>
    <manual>Open the map, toggle heatmap on, click a heatmap region — verify the popup shows a "View [name] details" link. Click it to navigate to the destination page. Click BackToMap — verify the map flies to the destination area.</manual>
  </verify>
  <done>
    - MapView accepts flyToTarget prop and calls map.flyTo() with animation when URL params are present
    - MapShell reads ?lat=&lng=&zoom= from URL and passes to MapView
    - Heatmap popup includes a link to the destination page
    - BackToMap from destination pages links back with destination coordinates for flyTo
  </done>
</task>

<task type="auto">
  <name>Task 2: Investigate and fix heatmap not displaying visually</name>
  <files>
    src/components/map/MapView.tsx
    src/lib/map/heatmap.ts
    supabase/functions/get_destinations_with_coords.sql
  </files>
  <action>
    The user reports the crowd heatmap is not visually displaying on the map despite code being wired. Investigate the following potential causes in order:

    **Investigation steps:**

    1. **Check if the RPC exists in Supabase:** Look at `supabase/functions/get_destinations_with_coords.sql` — this SQL function must be deployed to Supabase SQL Editor before the heatmap can load data. If the function is defined but may not have been deployed, add a clear console.warn in the fetchDestinations callback when the RPC returns an error, with the message: `"Heatmap disabled: run supabase/functions/get_destinations_with_coords.sql in Supabase SQL Editor"`.

    2. **Check if destinations data is empty:** In the `useEffect` that updates the heatmap source (line 552-563), add a console.log that shows `destinations.length` and the first feature's crowd_score so the developer can see if data is flowing. Remove or gate behind `process.env.NODE_ENV === 'development'` so it doesn't ship to production.

    3. **Check if heatmap-weight is zero for all features:** The heatmap-weight expression maps crowd_score 1 to weight 0 and crowd_score 10 to weight 1. If all destinations have a crowd_score of 1 (or near 1), the heatmap will be invisible because weight=0. Check if the default fallback `?? 5` is actually being reached (meaning crowd_data is null/undefined). If crowd_data is null for all destinations, the fallback of 5 should still produce a visible heatmap — so this is likely not the issue, but verify.

    4. **Check if the heatmap layer is being added before the style is loaded:** The `map.on('load', ...)` handler should ensure the style is loaded. Verify that `crowdHeatmapLayer` (which references source `'crowd-heatmap'`) is added AFTER `map.addSource('crowd-heatmap', ...)`. This is already the case in the code, but confirm.

    5. **Check heatmap-radius at global zoom levels:** At zoom 0, radius is 20px. With only ~30 destinations spread globally, each 20px blob may be too small to see. Consider increasing the minimum radius: change the heatmap-radius interpolation in `src/lib/map/heatmap.ts` from `[0, 20, 5, 40, 9, 60]` to `[0, 30, 3, 50, 6, 80, 9, 100]` so blobs are larger and more visible at low zoom levels where users typically first see the heatmap.

    6. **Check heatmap-opacity:** Current opacity is 0.6 which should be visible, but combined with the color ramp starting at `rgba(124, 58, 237, 0)` (fully transparent at density 0), low-density areas may appear invisible. This is expected behavior — only areas with overlapping destination points should show color.

    **Fixes to apply:**

    - Increase heatmap-radius values in `src/lib/map/heatmap.ts` so the heatmap is more visually prominent with only ~30 data points globally
    - Add defensive logging in `fetchDestinations` for developer debugging
    - Ensure the RPC error is surfaced clearly in the console so the user knows if deployment is needed
    - If the `get_destinations_with_coords` RPC query could fail silently (e.g., function doesn't exist), catch the error explicitly and log a helpful message

    **Do NOT change:** The heatmap color ramp, the toggle behavior, or the click interaction. Only fix visibility and debugging.
  </action>
  <verify>
    <automated>cd /Users/oliverhovey/Documents/vibes/HappeningNow && npx vitest run 2>&1 | tail -5</automated>
    <manual>Run `npm run dev`, open the map, toggle heatmap on. If the get_destinations_with_coords RPC is deployed, colored blobs should appear. If not, check browser console for the helpful error message. Verify blobs are visible at global zoom level.</manual>
  </verify>
  <done>
    - Heatmap radius values increased for visibility with sparse data points
    - Developer-facing console logging helps diagnose data pipeline issues
    - RPC errors surface clear instructions for deploying the SQL function
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
1. All existing tests pass: `npx vitest run`
2. TypeScript compiles: `npx tsc --noEmit`
3. MapView renders without errors when no URL search params are present (no regression)
4. MapView calls flyTo when ?lat=&lng=&zoom= params are provided
5. Heatmap popup contains link to destination page
6. Heatmap layer uses increased radius values for better visibility with sparse data
</verification>

<success_criteria>
- DEST-01 fully satisfied: destination pages trigger map flyTo animation on navigation
- Heatmap visibility improved with larger radius at low zoom levels
- Developer debugging improved with clear console messages for data pipeline issues
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-differentiators/03-04-SUMMARY.md`
</output>
