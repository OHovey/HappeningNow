---
phase: 03-visual-differentiators
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/destination/[slug]/page.tsx
  - src/components/destination/CalendarGrid.tsx
  - src/components/destination/MonthColumn.tsx
  - src/components/destination/BestTimeToVisit.tsx
  - src/components/destination/BookingWidget.tsx
  - src/components/destination/DestinationHero.tsx
  - src/lib/destination-utils.ts
autonomous: true
requirements:
  - DEST-01
  - DEST-02
  - DEST-03
  - DEST-04
  - DEST-05
  - DEST-06
  - DEST-07
  - PERF-01

must_haves:
  truths:
    - "User navigates to /destination/[slug] and sees a destination page with hero, calendar grid, and booking widget"
    - "The 12-month calendar grid shows crowd gradient bars, events, and weather data for each month"
    - "Crowd bars use the cool-to-warm gradient with the lowest-crowd months visually obvious"
    - "Weather summary per month shows temperature, rain days, and sunshine hours"
    - "2-3 best months are highlighted with a border and explanatory text above the grid"
    - "Event and wildlife pills in each month column expand to show details and affiliate links"
    - "Booking.com widget is pre-filled with destination and best month, loaded lazily"
    - "Mobile Lighthouse performance score is above 90"
  artifacts:
    - path: "src/app/destination/[slug]/page.tsx"
      provides: "SSG destination page with generateStaticParams and generateMetadata"
      exports: ["default", "generateStaticParams", "generateMetadata"]
    - path: "src/components/destination/CalendarGrid.tsx"
      provides: "12-month calendar grid with crowd/events/weather"
    - path: "src/components/destination/BookingWidget.tsx"
      provides: "Booking.com search box embed with lazy script loading"
    - path: "src/lib/destination-utils.ts"
      provides: "computeBestMonth and weather formatting utilities"
      exports: ["computeBestMonths", "formatTemperature", "formatWeatherSummary"]
  key_links:
    - from: "src/app/destination/[slug]/page.tsx"
      to: "src/lib/supabase/queries.ts"
      via: "getDestinationBySlug, getEventsByDestination, getAllDestinationSlugs"
      pattern: "import.*queries"
    - from: "src/components/destination/CalendarGrid.tsx"
      to: "src/lib/crowd-colors.ts"
      via: "crowdScoreToColor for month column gradient bars"
      pattern: "import.*crowd-colors"
    - from: "src/components/destination/BookingWidget.tsx"
      to: "flexiproduct.js"
      via: "next/script strategy='lazyOnload'"
      pattern: "Script.*flexiproduct"
---

<objective>
Create `/destination/[slug]` SSG pages with a 12-month calendar grid showing crowd levels, events, wildlife, and weather data per month, plus a Booking.com search widget and best-time-to-visit recommendations. Optimize for Lighthouse mobile score > 90.

Purpose: Destination pages are the content-rich drilldown that makes SEO pages non-thin. The calendar grid lets users instantly compare all 12 months at a glance, driving informed travel decisions and affiliate conversions.

Output: Static destination pages with hero, calendar grid, best-time-to-visit, event/wildlife pills, Booking.com widget, breadcrumbs, and back-to-map navigation. All performance-optimized for Lighthouse > 90.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-differentiators/03-RESEARCH.md
@.planning/phases/03-visual-differentiators/03-01-SUMMARY.md

@src/app/event/[slug]/page.tsx
@src/components/ui/Breadcrumbs.tsx
@src/components/ui/BackToMap.tsx
@src/components/panel/AffiliateLinks.tsx
@src/lib/crowd-colors.ts
@src/lib/supabase/types.ts
@src/lib/supabase/queries.ts
@src/lib/affiliates.ts
@src/data/seed/destinations.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create destination utility functions and all destination components</name>
  <files>
    src/lib/destination-utils.ts
    src/components/destination/DestinationHero.tsx
    src/components/destination/MonthColumn.tsx
    src/components/destination/CalendarGrid.tsx
    src/components/destination/BestTimeToVisit.tsx
    src/components/destination/BookingWidget.tsx
  </files>
  <action>
1. Create `src/lib/destination-utils.ts`:
   - `computeBestMonths(crowdData, weatherData): { months: number[], explanation: string }` — analyze crowd scores (lower is better) and weather (higher sunshine, lower rain, moderate temp) to recommend 2-3 best months. Weight crowd 60%, weather 40%. Return month numbers and a human-readable explanation like "March and April offer the best combination of pleasant weather and fewer crowds."
   - `formatTemperature(tempC: number): string` — return "27°C" format.
   - `formatWeatherSummary(weather: { temp_c, rain_days, sunshine_hours }): string` — e.g. "27°C · 3 rain days · 270h sun".

2. Create `src/components/destination/DestinationHero.tsx`:
   - Server component (no 'use client').
   - Props: `destination: DestinationWithCoords`.
   - Render destination name as h1, country/region as subtitle, a MiniMap showing the destination location (using existing MiniMap component with center at destination coords, interactive: false).
   - Use `next/image` with `priority` for any hero image (if destinations have images; if not, use the MiniMap as the hero visual).

3. Create `src/components/destination/MonthColumn.tsx`:
   - 'use client' component.
   - Props: `month: number`, `crowdScore: number`, `weather: { temp_c, rain_days, sunshine_hours } | null`, `events: Event[]`, `isBestMonth: boolean`.
   - Layout (per user decision — column info priority: crowd bar top, events middle, weather bottom):
     - Top: thin colored bar using `crowdScoreToColor(crowdScore)` from crowd-colors.ts (DEST-03). Per user decision: cool-to-warm gradient, doesn't interfere with readability.
     - Below bar: crowd score number and label (e.g. "3/10 · Low season").
     - Middle: event/wildlife pills — small colored pills showing event names. Clicking a pill expands it to show brief description + affiliate link. Use the existing affiliate link builders. Pills are orange for festivals, green for wildlife (matching map colors). Per user decision: pills expand to show details + affiliate links (DEST-06).
     - Bottom: weather data — temperature, rain days, sunshine hours formatted compactly.
   - If `isBestMonth`, add a subtle highlighted border (e.g. ring-2 ring-emerald-400) per user decision.
   - Month name as column header (Jan, Feb, etc.).

4. Create `src/components/destination/CalendarGrid.tsx`:
   - 'use client' component.
   - Props: `destination: DestinationWithCoords`, `events: Event[]`.
   - Render 12 MonthColumn components in a CSS Grid row.
   - Desktop: `grid-cols-12` — all 12 months visible in a single horizontal strip per user decision.
   - Mobile responsive collapse: use `grid-cols-4` on small screens (3 rows of 4) or `grid-cols-3` on very small. Per Claude's discretion on exact collapse behavior.
   - Pass correct data to each column: `crowdScore` from `destination.crowd_data?.[String(month)]`, `weather` from `destination.weather_data?.[String(month)]`, `events` filtered to those active in this month (where month falls between start_month and end_month, handling wrap-around).
   - Compute best months using `computeBestMonths()` to determine which columns get the highlighted border.

5. Create `src/components/destination/BestTimeToVisit.tsx`:
   - Server component.
   - Props: `crowdData`, `weatherData`.
   - Call `computeBestMonths()` and render the recommended months with explanation text.
   - Highlighted months shown as colored pills. Short sentence explanation above the grid per user decision.

6. Create `src/components/destination/BookingWidget.tsx`:
   - 'use client' component.
   - Props: `destId: string | null`, `destName: string`, `bestMonth: number`.
   - Render the Booking.com `<ins>` embed tag with `data-aid`, `data-prod="nsb"`, `data-dest_id`, `data-dest_type="city"`, `data-width="100%"`.
   - Use `process.env.NEXT_PUBLIC_BOOKING_AFFILIATE_ID` for `data-aid`. If not set, render a fallback deep link using the existing `buildBookingLink()` function.
   - Load `//aff.bstatic.com/static/affiliate_base/js/flexiproduct.js` via `next/script` with `strategy="lazyOnload"` — critical for PERF-01 (Lighthouse mobile > 90). Per research: eager loading tanks TBT.
   - Include a static fallback `<a>` link inside the `<ins>` tag so the CTA is visible before the script loads.
   - Wrap in an FtcDisclosure component (reuse existing).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
    <manual>Verify computeBestMonths returns sensible month recommendations for sample destination data</manual>
  </verify>
  <done>
    - computeBestMonths picks 2-3 months with lowest crowd + best weather combination
    - CalendarGrid renders 12 columns with crowd gradient, events, and weather
    - MonthColumn shows crowd bar, event pills, and weather in correct priority order
    - BookingWidget loads flexiproduct.js lazily with static fallback
    - Best months highlighted with border in the grid
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /destination/[slug] SSG page with metadata, JSON-LD, and Lighthouse optimization</name>
  <files>
    src/app/destination/[slug]/page.tsx
  </files>
  <action>
1. Create `src/app/destination/[slug]/page.tsx` following the exact pattern of `src/app/event/[slug]/page.tsx`:
   - `generateStaticParams()` — call `getAllDestinationSlugs()`.
   - `generateMetadata({ params })` — fetch destination, return title, description, OG tags. Title: "{Destination Name} — Best Times to Visit | HappeningNow". Description: summarize crowd and event data.
   - Default async server component:
     - Await params, fetch destination via `getDestinationBySlug(slug)`.
     - If not found, call `notFound()`.
     - Fetch events via `getEventsByDestination(destination)`.
     - Render page layout:
       ```
       <Breadcrumbs items={[{ label: 'Home', href: '/' }, { label: 'Destinations', href: '/destinations' }, { label: destination.name }]} />
       <DestinationHero destination={destination} />
       <BestTimeToVisit crowdData={destination.crowd_data} weatherData={destination.weather_data} />
       <CalendarGrid destination={destination} events={events} />
       <BookingWidget destId={destination.booking_destination_id} destName={destination.name} bestMonth={computeBestMonths(destination.crowd_data, destination.weather_data).months[0]} />
       <BackToMap />
       ```
     - Add JSON-LD structured data for TouristDestination or Place schema type.

2. Lighthouse optimization for PERF-01 (mobile > 90):
   - Use `next/font` for any custom fonts (project likely already uses it).
   - Ensure hero MiniMap or image uses `priority` prop for LCP.
   - BookingWidget script is already `lazyOnload`.
   - Avoid layout shift: set explicit dimensions on the MiniMap container and CalendarGrid.
   - Ensure all images use `next/image` with proper `width`/`height` or `fill`.

3. Verify the Destination type has a `booking_destination_id` field. Check the seed data. If destinations don't have this field, add it to the TypeScript type and handle null gracefully in BookingWidget (show fallback link with destination name as search term).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
    <manual>Run `npm run dev`, navigate to `/destination/[any-slug]` — see hero, 12-month grid with crowd colors, weather data, event pills, booking widget. Run `npx lighthouse http://localhost:3000/destination/bali --only-categories=performance --output=json | jq '.categories.performance.score'` to check mobile score > 0.9.</manual>
  </verify>
  <done>
    - /destination/[slug] pages render with full calendar grid, hero, breadcrumbs
    - generateStaticParams produces all destination slugs
    - generateMetadata sets correct title, description, OG tags
    - JSON-LD structured data present in page source
    - BookingWidget renders with lazy script loading
    - Lighthouse mobile performance score > 90
    - Back to map button navigates to homepage
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` passes with no regressions
- `/destination/[slug]` pages are statically generated at build time
- Calendar grid shows 12 months with correct crowd gradient, weather, and events
- Best-time-to-visit highlights 2-3 recommended months
- Booking.com widget renders with lazy loading
- Mobile Lighthouse score > 90 on destination pages
- All destination pages have breadcrumbs, JSON-LD, OG tags
</verification>

<success_criteria>
All seven DEST requirements (01-07) and PERF-01 are implemented. Destination pages are scannable at a glance — users can instantly see crowd patterns across a full year. The calendar grid uses the shared crowd-colors from Plan 03-01. Booking.com widget loads lazily, keeping Lighthouse mobile score above 90.
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-differentiators/03-03-SUMMARY.md`
</output>
