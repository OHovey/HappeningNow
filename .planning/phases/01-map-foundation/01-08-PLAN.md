---
phase: 01-map-foundation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/lib/convertkit.ts
autonomous: true
gap_closure: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05
  - MAP-06
  - MAP-07
  - MAP-08
  - MAP-09
  - AFFL-01
  - AFFL-02
  - AFFL-03
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - PERF-04
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "MAP-07 requirement text matches the actual implementation (month pills, not dropdown)"
    - "TAG_IDS in convertkit.ts reads from environment variables so tagging works without code changes once Kit tags are created"
    - "REQUIREMENTS.md accurately reflects what was built in Phase 1"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated MAP-07 description matching pills implementation"
      contains: "Month pills"
    - path: "src/lib/convertkit.ts"
      provides: "Environment-variable-driven tag IDs"
      contains: "process.env"
  key_links:
    - from: "src/lib/convertkit.ts"
      to: "process.env.KIT_TAG_FESTIVALS / KIT_TAG_WILDLIFE"
      via: "parseInt of env var with fallback to 0"
      pattern: "process\\.env\\.KIT_TAG"
---

<objective>
Close three verification gaps from Phase 1 verification.

Purpose: Resolve the 3 gaps identified in 01-VERIFICATION.md so Phase 1 reaches full verification (22/22). Two gaps are documentation corrections (MAP-07 wording, MapControls.tsx reference). One gap is a configuration improvement (EMAIL-03 TAG_IDS should read from env vars instead of requiring code changes).

Output: Updated REQUIREMENTS.md, updated convertkit.ts with env-var-driven tag IDs.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-foundation/01-VERIFICATION.md
@src/lib/convertkit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md to reflect actual Phase 1 implementation</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Fix two documentation gaps in REQUIREMENTS.md:

1. **MAP-07**: Change the description from "Month picker dropdown above the map for direct month selection" to "Month picker pills above the map for direct month selection (tappable month buttons, horizontally scrollable on mobile)". This aligns with the user's locked decision in CONTEXT.md ("Month pills/tabs (Jan, Feb, Mar...) — tappable buttons, not a drag slider") and the RESEARCH.md architectural spec ("month pills UI, filter update on click").

2. **EMAIL-03**: Add a parenthetical note clarifying the configuration requirement. Change "Interest-based tagging on subscribe (festivals / wildlife / region preferences)" to "Interest-based tagging on subscribe (festivals / wildlife / region preferences) — requires KIT_TAG_FESTIVALS and KIT_TAG_WILDLIFE env vars from Kit dashboard".

Do NOT change any checkbox states — all Phase 1 requirements remain checked [x].
  </action>
  <verify>
    <automated>grep -n "MAP-07" .planning/REQUIREMENTS.md | grep -q "pills" && grep -n "EMAIL-03" .planning/REQUIREMENTS.md | grep -q "KIT_TAG" && echo "PASS" || echo "FAIL"</automated>
    <manual>Verify MAP-07 says "pills" not "dropdown", and EMAIL-03 mentions env vars</manual>
  </verify>
  <done>REQUIREMENTS.md MAP-07 description says "month picker pills" matching the locked user decision and actual implementation. EMAIL-03 description notes the env var configuration requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Make TAG_IDS read from environment variables</name>
  <files>src/lib/convertkit.ts</files>
  <action>
Update the TAG_IDS definition in src/lib/convertkit.ts to read tag IDs from environment variables instead of hardcoded zeros. This eliminates the need for code changes when configuring Kit tags.

Replace the current:
```typescript
export const TAG_IDS: Record<string, number> = {
  festivals: 0,
  wildlife: 0,
};
```

With:
```typescript
export const TAG_IDS: Record<string, number> = {
  festivals: parseInt(process.env.KIT_TAG_FESTIVALS || '0', 10),
  wildlife: parseInt(process.env.KIT_TAG_WILDLIFE || '0', 10),
};
```

Update the JSDoc comment above TAG_IDS to say:
```
/**
 * Tag IDs from Kit Dashboard -> Subscribers -> Tags.
 * Set KIT_TAG_FESTIVALS and KIT_TAG_WILDLIFE environment variables.
 * When set to 0 (default), tagging is silently skipped.
 */
```

The existing `if (tagId === 0) return;` guard in tagSubscriber() remains unchanged — it correctly skips unconfigured tags.

Do NOT modify any other code in this file. The createSubscriber and tagSubscriber functions stay exactly as they are.
  </action>
  <verify>
    <automated>cd /Users/oliverhovey/Documents/vibes/HappeningNow && grep -q "process.env.KIT_TAG_FESTIVALS" src/lib/convertkit.ts && grep -q "process.env.KIT_TAG_WILDLIFE" src/lib/convertkit.ts && npx vitest run tests/api/subscribe.test.ts --reporter=verbose 2>&1 | tail -5</automated>
    <manual>Confirm TAG_IDS reads from env vars and existing subscribe tests still pass</manual>
  </verify>
  <done>TAG_IDS reads KIT_TAG_FESTIVALS and KIT_TAG_WILDLIFE from environment variables with fallback to 0. Existing tests pass. Tagging will activate automatically when env vars are set — no code change needed.</done>
</task>

</tasks>

<verification>
1. `grep "pills" .planning/REQUIREMENTS.md` returns the updated MAP-07 line
2. `grep "KIT_TAG" src/lib/convertkit.ts` shows env var reads for both tag IDs
3. `npx vitest run tests/api/subscribe.test.ts` passes (existing tests unbroken)
4. Gap 3 (MapControls.tsx) is informational only — no code or doc change needed since MAP-09 is fully satisfied inline in MapView.tsx
</verification>

<success_criteria>
- REQUIREMENTS.md MAP-07 says "Month picker pills" instead of "Month picker dropdown"
- EMAIL-03 description notes the env var requirement
- convertkit.ts TAG_IDS reads from process.env.KIT_TAG_FESTIVALS and process.env.KIT_TAG_WILDLIFE
- All existing tests pass
- All 3 verification gaps are addressed (2 by code/doc changes, 1 acknowledged as informational)
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-foundation/01-08-SUMMARY.md`
</output>
