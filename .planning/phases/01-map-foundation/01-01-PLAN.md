---
phase: 01-map-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/ui/ErrorBoundary.tsx
  - src/components/ui/LoadingSkeleton.tsx
  - vitest.config.ts
  - tests/components/error-boundary.test.ts
autonomous: true
requirements:
  - FOUND-01
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "Next.js 15 project builds successfully with `npm run build`"
    - "Site deploys to Vercel and serves a page"
    - "Plausible analytics script loads in production"
    - "Error boundary catches and displays component errors gracefully"
    - "Loading skeleton renders as placeholder while content loads"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout with PlausibleProvider"
      contains: "PlausibleProvider"
    - path: "src/app/page.tsx"
      provides: "Home page shell"
      min_lines: 5
    - path: "src/components/ui/ErrorBoundary.tsx"
      provides: "React error boundary wrapper"
      exports: ["ErrorBoundary"]
    - path: "src/components/ui/LoadingSkeleton.tsx"
      provides: "Loading skeleton placeholder"
      exports: ["LoadingSkeleton"]
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
  key_links:
    - from: "src/app/layout.tsx"
      to: "PlausibleProvider"
      via: "next-plausible provider wrapping children"
      pattern: "PlausibleProvider"
    - from: "src/components/ui/ErrorBoundary.tsx"
      to: "React.Component"
      via: "componentDidCatch lifecycle"
      pattern: "componentDidCatch"
---

<objective>
Scaffold the Next.js 15 project with TypeScript, Tailwind CSS v4, and App Router. Set up Plausible analytics, reusable error boundaries, loading skeletons, and the Vitest test framework.

Purpose: Establish the project foundation that all other plans build on — framework, styling, testing, analytics, and error handling infrastructure.
Output: Deployable Next.js 15 app on Vercel with analytics, error boundaries, loading states, and test runner configured.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with Tailwind v4 and core dependencies</name>
  <files>
    package.json
    tsconfig.json
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    vitest.config.ts
  </files>
  <action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` (use `.` since the directory already exists — if it fails because non-empty, create in a temp dir and move files).

Install additional dependencies:
```bash
npm install maplibre-gl @supabase/supabase-js next-plausible
npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom
```

Configure Tailwind v4 CSS-first in `src/app/globals.css`:
- Use `@import "tailwindcss"` (NOT a tailwind.config.js — v4 uses CSS-first config)
- Add `@theme` block for custom colors: festival orange (#f97316), wildlife green (#22c55e), other blue (#3b82f6)

Configure `vitest.config.ts`:
- Use `@vitejs/plugin-react`
- Set environment to `jsdom`
- Configure path aliases to match tsconfig (`@/*` -> `./src/*`)
- Include `tests/**/*.test.ts` and `tests/**/*.test.tsx`

Update `src/app/layout.tsx`:
- Wrap children with `PlausibleProvider` from `next-plausible`
- Use domain `happeningnow.travel` (can be env var)
- Set `customDomain` for self-hosting proxy if available, otherwise use default
- Add basic metadata: title "HappeningNow — Explore What's Happening Around the World", description about the timeline map

Update `src/app/page.tsx`:
- Simple placeholder page with "HappeningNow" heading
- This will be replaced by MapShell in plan 01-04

Create `.env.local.example` with all env var placeholders:
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
KIT_API_KEY=
NEXT_PUBLIC_BOOKING_AFFILIATE_ID=
NEXT_PUBLIC_GYG_PARTNER_ID=
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=happeningnow.travel
```

Add `test` script to package.json: `"test": "vitest run"`
  </action>
  <verify>
    <automated>npm run build && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
    <manual>Check that build completes without errors</manual>
  </verify>
  <done>Next.js 15 project builds successfully. Vitest runs (0 tests initially). Package.json has all required dependencies. Tailwind v4 configured via CSS-first approach. PlausibleProvider wraps the app.</done>
</task>

<task type="auto">
  <name>Task 2: Create error boundary, loading skeleton, and their tests</name>
  <files>
    src/components/ui/ErrorBoundary.tsx
    src/components/ui/LoadingSkeleton.tsx
    tests/components/error-boundary.test.ts
  </files>
  <action>
Create `src/components/ui/ErrorBoundary.tsx`:
- React class component (error boundaries require componentDidCatch)
- `'use client'` directive (error boundaries must be client components)
- Props: `children`, `fallback` (optional custom fallback component)
- State: `hasError: boolean`, `error: Error | null`
- `componentDidCatch`: log error to console (Plausible custom event tracking can be added later)
- Default fallback UI: centered card with "Something went wrong" message, error message text, and "Try again" button that resets state
- Export named `ErrorBoundary`

Create `src/components/ui/LoadingSkeleton.tsx`:
- `'use client'` directive
- Props: `className?: string`, `variant?: 'map' | 'card' | 'text'`
- Map variant: `h-screen w-full animate-pulse bg-gray-900` (dark gray, full viewport — for map loading)
- Card variant: rounded rectangle with shimmer
- Text variant: horizontal bars with varying widths
- Default variant: generic rectangle with `animate-pulse`

Create `tests/components/error-boundary.test.ts`:
- Test that ErrorBoundary renders children when no error
- Test that ErrorBoundary renders fallback when child throws
- Test that "Try again" button resets error state
- Use `@testing-library/react` for rendering
  </action>
  <verify>
    <automated>npx vitest run tests/components/error-boundary.test.ts --reporter=verbose</automated>
    <manual>Verify error boundary catches errors and shows fallback UI</manual>
  </verify>
  <done>ErrorBoundary catches component errors and displays a retry UI. LoadingSkeleton renders appropriate placeholder variants. Error boundary test passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx vitest run` passes all tests (error boundary tests)
- `src/app/layout.tsx` includes PlausibleProvider
- `.env.local.example` documents all required environment variables
</verification>

<success_criteria>
A deployable Next.js 15 application with TypeScript, Tailwind v4, Plausible analytics, error boundaries, loading skeletons, and Vitest test runner — ready for map and database plans to build on.
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-foundation/01-01-SUMMARY.md`
</output>
