---
phase: 04-programmatic-seo
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/wildlife/region/[region]/page.tsx
  - src/app/wildlife/region/[region]/[month]/page.tsx
  - src/app/wildlife/species/[species]/page.tsx
  - src/app/event/[slug]/page.tsx
  - src/app/wildlife/[slug]/page.tsx
autonomous: true
requirements:
  - SEO-04
  - SEO-05
  - SEO-06
  - PAGE-01
  - PAGE-02

must_haves:
  truths:
    - "User visits /wildlife/region/africa and sees wildlife viewing opportunities across Africa"
    - "User visits /wildlife/species/humpback-whale and sees all viewing locations for humpback whales worldwide"
    - "User visits /wildlife/region/africa/6 and sees June wildlife in Africa"
    - "Existing /event/[slug] pages have canonical URLs in metadata"
    - "Existing /wildlife/[slug] pages have canonical URLs in metadata"
    - "Wildlife SEO pages use sub-paths (/wildlife/region/, /wildlife/species/) to avoid conflict with existing /wildlife/[slug] detail pages"
  artifacts:
    - path: "src/app/wildlife/region/[region]/page.tsx"
      provides: "Wildlife region listing pages"
      exports: ["default", "generateMetadata", "generateStaticParams", "revalidate", "dynamicParams"]
    - path: "src/app/wildlife/species/[species]/page.tsx"
      provides: "Wildlife species listing pages"
      exports: ["default", "generateMetadata", "generateStaticParams", "revalidate", "dynamicParams"]
    - path: "src/app/wildlife/region/[region]/[month]/page.tsx"
      provides: "Wildlife region+month listing pages"
      exports: ["default", "generateMetadata", "generateStaticParams", "revalidate", "dynamicParams"]
  key_links:
    - from: "src/app/wildlife/region/[region]/page.tsx"
      to: "src/lib/supabase/seo-queries.ts"
      via: "getWildlifeByRegion query"
      pattern: "getWildlifeByRegion"
    - from: "src/app/wildlife/species/[species]/page.tsx"
      to: "src/lib/supabase/seo-queries.ts"
      via: "getWildlifeBySpecies query"
      pattern: "getWildlifeBySpecies"
    - from: "src/app/event/[slug]/page.tsx"
      to: "generateMetadata"
      via: "canonical URL in alternates"
      pattern: "alternates.*canonical"
---

<objective>
Create 3 wildlife programmatic SEO route families using sub-paths to avoid conflict with existing `/wildlife/[slug]` detail pages, and enhance existing event and wildlife detail pages with canonical URLs.

Purpose: Wildlife SEO pages (~90 pages) target "wildlife viewing [region]" and "[species] migration" search queries. Canonical URLs on existing detail pages (PAGE-01, PAGE-02) prevent duplicate content issues.
Output: 3 wildlife SEO page routes under `/wildlife/region/` and `/wildlife/species/`, plus canonical URL enhancements to existing detail pages.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-programmatic-seo/04-RESEARCH.md
@.planning/phases/04-programmatic-seo/04-01-SUMMARY.md
@src/app/event/[slug]/page.tsx
@src/app/wildlife/[slug]/page.tsx
@src/lib/seo/intro-templates.ts
@src/lib/supabase/seo-queries.ts
@src/components/seo/SeoPageLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wildlife region and species SEO pages</name>
  <files>
    src/app/wildlife/region/[region]/page.tsx
    src/app/wildlife/region/[region]/[month]/page.tsx
    src/app/wildlife/species/[species]/page.tsx
  </files>
  <action>
    Per RESEARCH.md Pattern 2 (Route Disambiguation), use sub-paths `/wildlife/region/` and `/wildlife/species/` to avoid conflict with existing `/wildlife/[slug]` migration route detail pages.

    **wildlife/region/[region]/page.tsx (SEO-04):**
    ```
    export const revalidate = 86400;
    export const dynamicParams = true;
    export async function generateStaticParams() { return []; }
    ```
    1. Await params, extract region slug
    2. Fetch wildlife events by region using getWildlifeByRegion()
    3. If no events found, notFound()
    4. Use shouldNoindex() for thin page check
    5. Use getWildlifeRegionIntro() for intro text
    6. Canonical URL: `https://happeningnow.travel/wildlife/region/${region}`
    7. Render SeoPageLayout > FilteredMap + EventCardGrid
    8. Internal links: links to region+month pages, other regions with wildlife
    9. Emphasize seasons and viewing locations per user decision (wildlife template focuses on seasons/locations/tour operators)
    10. EventCardGrid: show GetYourGuide CTAs (species-based tour search). Hide Booking.com CTAs for wildlife per Phase 2 decision.

    **wildlife/region/[region]/[month]/page.tsx (SEO-06):**
    Same ISR pattern. Filter wildlife by region AND month.
    1. Validate month 1-12
    2. Fetch using getWildlifeByRegionMonth()
    3. Use getWildlifeRegionMonthIntro() for intro
    4. Canonical URL: `https://happeningnow.travel/wildlife/region/${region}/${month}`
    5. Render same component structure as region page but filtered to month

    **wildlife/species/[species]/page.tsx (SEO-05):**
    Same ISR pattern. Show all viewing locations for a single species worldwide.
    1. Fetch using getWildlifeBySpecies()
    2. Use getWildlifeSpeciesIntro() for intro
    3. Canonical URL: `https://happeningnow.travel/wildlife/species/${species}`
    4. Group events by region for clear geographic organization
    5. Internal links: other species in same regions, species detail page link

    All wildlife SEO pages use semantic HTML with h1, h2 (per region/species/month), article tags per AIDX-03. Wildlife cards should emphasize: best viewing months, peak dates, species name, and "Find Tours" GYG CTA button (no Booking.com per Phase 2 wildlife decision).
  </action>
  <verify>
    <automated>cd /Users/oliverhovey/Documents/vibes/HappeningNow && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify src/app/wildlife/region/ and src/app/wildlife/species/ directories exist with page.tsx files</manual>
  </verify>
  <done>Wildlife region, region/month, and species pages render with correct intro templates, ISR config, filtered events, GetYourGuide CTAs (no Booking.com), and canonical URLs</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical URLs to existing event and wildlife detail pages</name>
  <files>
    src/app/event/[slug]/page.tsx
    src/app/wildlife/[slug]/page.tsx
  </files>
  <action>
    Enhance existing detail pages to satisfy PAGE-01 and PAGE-02 by adding canonical URLs to their generateMetadata functions.

    **event/[slug]/page.tsx:**
    In the existing generateMetadata function, add `alternates.canonical`:
    ```typescript
    alternates: {
      canonical: `https://happeningnow.travel/event/${slug}`,
    }
    ```
    This ensures Google treats /event/[slug] as the canonical source for event content, preventing any programmatic page from being seen as a duplicate.

    **wildlife/[slug]/page.tsx:**
    Same pattern — add canonical URL to generateMetadata:
    ```typescript
    alternates: {
      canonical: `https://happeningnow.travel/wildlife/${slug}`,
    }
    ```

    Do NOT change any other functionality in these existing pages. This is a metadata-only enhancement.
  </action>
  <verify>
    <automated>cd /Users/oliverhovey/Documents/vibes/HappeningNow && npx vitest run tests/metadata.test.ts tests/structured-data.test.ts</automated>
    <manual>Check that generateMetadata in both files includes alternates.canonical</manual>
  </verify>
  <done>Existing /event/[slug] and /wildlife/[slug] detail pages include canonical URLs in their metadata, existing tests still pass</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles with no errors
- `npx vitest run tests/` all existing tests pass (no regressions)
- Wildlife SEO pages use `/wildlife/region/` and `/wildlife/species/` sub-paths (no route conflict)
- Event and wildlife detail pages have canonical URLs
- Wildlife pages show GetYourGuide CTAs only (no Booking.com)
</verification>

<success_criteria>
- All 3 wildlife route families functional via ISR
- No route conflict with existing /wildlife/[slug] pages
- Existing detail pages enhanced with canonical URLs
- Wildlife affiliate CTAs are GetYourGuide-only
- Each page has unique intro text from templates
</success_criteria>

<output>
After completion, create `.planning/phases/04-programmatic-seo/04-03-SUMMARY.md`
</output>
