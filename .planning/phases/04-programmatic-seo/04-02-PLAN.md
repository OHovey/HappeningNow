---
phase: 04-programmatic-seo
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/festivals/[region]/[month]/page.tsx
  - src/app/festivals/[country]/page.tsx
  - src/app/festivals/[country]/[month]/page.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-12

must_haves:
  truths:
    - "User visits /festivals/southeast-asia/march and sees filtered festival events for that region and month with intro text, map, event cards, and affiliate links"
    - "User visits /festivals/thailand and sees all festivals in Thailand with calendar view and event cards"
    - "User visits /festivals/thailand/3 and sees March festivals in Thailand"
    - "Pages with fewer than 3 events have noindex meta but still render content and follow links"
    - "All festival pages export revalidate = 86400 and dynamicParams = true for ISR"
  artifacts:
    - path: "src/app/festivals/[region]/[month]/page.tsx"
      provides: "Festival region+month SEO pages"
      exports: ["default", "generateMetadata", "generateStaticParams", "revalidate", "dynamicParams"]
    - path: "src/app/festivals/[country]/page.tsx"
      provides: "Festival country SEO pages"
      exports: ["default", "generateMetadata", "generateStaticParams", "revalidate", "dynamicParams"]
    - path: "src/app/festivals/[country]/[month]/page.tsx"
      provides: "Festival country+month SEO pages"
      exports: ["default", "generateMetadata", "generateStaticParams", "revalidate", "dynamicParams"]
  key_links:
    - from: "src/app/festivals/[region]/[month]/page.tsx"
      to: "src/lib/supabase/seo-queries.ts"
      via: "getFestivalsByRegionMonth query"
      pattern: "getFestivalsByRegionMonth"
    - from: "src/app/festivals/[country]/page.tsx"
      to: "src/lib/seo/intro-templates.ts"
      via: "getFestivalCountryIntro template"
      pattern: "getFestivalCountryIntro"
    - from: "festival page.tsx files"
      to: "src/components/seo/SeoPageLayout.tsx"
      via: "shared layout import"
      pattern: "SeoPageLayout"
---

<objective>
Create the 3 festival programmatic SEO route families: `/festivals/[region]/[month]` (~120 pages), `/festivals/[country]` (~80 pages), and `/festivals/[country]/[month]` (~960 pages). Each page uses ISR with revalidate=86400, empty generateStaticParams, and the shared SEO components from Plan 01.

Purpose: Festival pages are the largest SEO page family (~1,160 pages) and target high-intent "festivals in [country] [month]" search queries.
Output: 3 Next.js App Router page files with generateMetadata, ISR config, and full page rendering.
</objective>

<execution_context>
@/Users/oliverhovey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oliverhovey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-programmatic-seo/04-RESEARCH.md
@.planning/phases/04-programmatic-seo/04-01-SUMMARY.md
@src/lib/seo/intro-templates.ts
@src/lib/seo/thin-page-check.ts
@src/lib/supabase/seo-queries.ts
@src/components/seo/SeoPageLayout.tsx
@src/components/seo/EventCardGrid.tsx
@src/components/seo/FilteredMap.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /festivals/[region]/[month] and /festivals/[country] pages</name>
  <files>
    src/app/festivals/[region]/[month]/page.tsx
    src/app/festivals/[country]/page.tsx
  </files>
  <action>
    **Route disambiguation note:** The `/festivals/[region]/[month]` and `/festivals/[country]/[month]` routes share the same pattern `[param1]/[param2]`. Disambiguate by checking if param1 matches a known region (e.g., "southeast-asia", "europe", "africa", "south-america", "north-america", "oceania", "middle-east", "central-asia") vs a country slug. In the `[region]/[month]` page, validate that param1 is a known region AND param2 is a valid month (1-12); if not, call `notFound()`. In `[country]/[month]`, validate that param1 is NOT a known region; if it is a region, call `notFound()`.

    IMPORTANT: Since Next.js cannot have two `[param]/[param]` routes at the same directory level, use a SINGLE `[slug]/[month]` directory structure that internally disambiguates. Create:
    - `src/app/festivals/[slug]/page.tsx` — country page (renders when no month sub-path)
    - `src/app/festivals/[slug]/[month]/page.tsx` — disambiguates region+month vs country+month internally

    **festivals/[slug]/[month]/page.tsx (handles both SEO-01 and SEO-03):**
    ```
    export const revalidate = 86400;
    export const dynamicParams = true;
    export async function generateStaticParams() { return []; }
    ```

    In generateMetadata and the page component:
    1. Parse `slug` and `month` from params (await params per Next.js 16 pattern)
    2. Validate month is 1-12, else notFound()
    3. Check if slug matches a known region — if yes, use region template; if no, treat as country
    4. Fetch events using appropriate seo-query function
    5. Use shouldNoindex() to conditionally set robots.index=false
    6. Generate canonical URL: `https://happeningnow.travel/festivals/${slug}/${month}`
    7. Use appropriate intro template (region vs country variant)

    Page component renders: SeoPageLayout > FilteredMap + EventCardGrid + InternalLinks

    **festivals/[slug]/page.tsx (SEO-02 — country page):**
    Same ISR pattern. This page shows ALL festivals in a country across all months.
    1. Validate slug is a valid country (not a region), else notFound()
    2. Fetch all festivals for the country using getFestivalsByCountry()
    3. Group events by month for a calendar-style display
    4. Use getFestivalCountryIntro() for intro text
    5. Generate canonical URL: `https://happeningnow.travel/festivals/${slug}`
    6. Render SeoPageLayout with EventCardGrid grouped by month sections (h2 per month)
    7. Internal links: links to country+month pages, nearby countries

    Both pages must use semantic HTML: h1 for page title, h2 for month sections, article tags for event cards, data-section attributes per AIDX-03.
  </action>
  <verify>
    <automated>cd /Users/oliverhovey/Documents/vibes/HappeningNow && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify src/app/festivals/[slug]/page.tsx and src/app/festivals/[slug]/[month]/page.tsx exist and export revalidate, dynamicParams, generateStaticParams, generateMetadata</manual>
  </verify>
  <done>Festival region/month pages render filtered events with correct intro templates, festival country pages show all-month calendar view, ISR configured with revalidate=86400, thin pages get noindex, canonical URLs set</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles with no errors
- Festival pages export `revalidate = 86400` and `dynamicParams = true`
- `generateStaticParams()` returns empty array in both routes
- Region vs country disambiguation works (region slugs → region template, country slugs → country template)
- Pages use SeoPageLayout, EventCardGrid, FilteredMap from Plan 01
</verification>

<success_criteria>
- All 3 festival route families functional via ISR
- Thin pages (< 3 events, no supplementary data) get noindex
- Each page has unique intro text from 20+ template variations
- Canonical URLs set correctly
- Pages render event cards with bold affiliate CTAs
</success_criteria>

<output>
After completion, create `.planning/phases/04-programmatic-seo/04-02-SUMMARY.md`
</output>
